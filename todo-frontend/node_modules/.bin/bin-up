#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../bin-up/bin/bin-up" "$@"
  ret=$?
else 
  node  "$basedir/../bin-up/bin/bin-up" "$@"
  ret=$?
fi
exit $ret
